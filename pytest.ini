[pytest]
testpaths = evals
python_files = test_*.py
markers =
    ci: fast custom checks (guardrails, injection, router)
    nightly: DeepEval single-turn agent metrics
    memory: DeepEval multi-turn conversation metrics
    live: requires EVAL_LIVE=1 and full API/MCP stack
